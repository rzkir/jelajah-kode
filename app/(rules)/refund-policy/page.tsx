import RefundPolicyPage from '@/components/pages/rules/refund-policy/RefundPolicy'

import { RefundPolicyMetadata } from '@/helper/meta/Metadata'

export const metadata = RefundPolicyMetadata

export default function page() {
    return (
        <RefundPolicyPage />
    )
}
